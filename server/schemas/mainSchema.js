const mongoose = require('mongoose');
/*
This schema will be the best schema - and let me tell you  there are some nice schemas, but this schema will be the best. Simply the best schema on earth that I think weve probably ever seen. The dems want to use foreign keys I say we have all the keys we need right here in this schema. Thats what I tell em.

*/
let Schema = mongoose.Schema;

const MainSchema = new mongoose.Schema({

})

const Main = mongoose.model('Main', MainSchema);

module.exports = Main;